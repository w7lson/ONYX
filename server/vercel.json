{
  "version": 2,
  "builds": [
    {
      "src": "src/index.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/**", "prisma/**", "node_modules/.prisma/client/**"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/index.js" }
  ]
}
